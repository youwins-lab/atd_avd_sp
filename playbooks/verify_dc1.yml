---
- name: Validate states on DC1 EOS devices with ANTA
  hosts: dc1_fabric
  connection: local
  gather_facts: false

  tasks:

    - name: Run ANTA on EOS devices
      ansible.builtin.import_role:
        name: arista.avd.anta_runner

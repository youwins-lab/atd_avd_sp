---
# 설정 변경 없이 현재 동작 상태만 ANTA 로 검증합니다.
- name: Validate states on MPLS-SR DCI Lab devices
  hosts: MPLS_SR_DCI_FABRIC
  connection: local
  gather_facts: false
  tasks:
    - name: Run ANTA on EOS devices
      ansible.builtin.import_role:
        name: arista.avd.anta_runner

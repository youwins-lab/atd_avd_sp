---
- name: Validate states on DC2 EOS devices with ANTA
  hosts: dc2_fabric
  connection: local
  gather_facts: false

  tasks:

    - name: Run ANTA on EOS devices
      ansible.builtin.import_role:
        name: arista.avd.anta_runner
